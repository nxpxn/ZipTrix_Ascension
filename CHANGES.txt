2026-07-31
- Added custom search filters for "Realm Bound" items (keywords: realm bound, realmbound, rb) for inventory addons that use LibItemSearch-1.0 (such as Bagnon).

2026-07-22
- Added custom Project Ascension search filters (worldforged, wf, mythic, fel-forged, felforged, awoken, titanforged) for inventory addons that use LibItemSearch-1.0 (such as Bagnon).
- Implemented a new Bagnon Keyword Filters drawer that attaches directly to the Bagnon inventory frame.
- The drawer automatically tracks and remembers your 5 most recent search history terms.
- Added Pin/Unpin support (Right-Click) to permanently save favorite keyword searches.
- Quick Filter Toggle: Left-clicking a keyword automatically filters the bag. Clicking the active keyword again will instantly clear the search filter.
- The drawer dynamically inherits the frame color and border color of your Bagnon settings.
- The drawer position can be cycled between the Top, Bottom, Left, and Right edges of the Bagnon frame.
- Added a configuration checkbox in the ZipTrix interface options to enable or disable the Bagnon Filters Drawer.

2026-07-13
- Initial implementation of the ZipTrix addon.
- Added "MoveAnything and ElvUI fix" to resolve Character Frame dragging issues when both addons are loaded.
- Moved the configuration UI from a standalone floating window into the standard World of Warcraft Interface Options (AddOns tab).
- Added UI placeholders for upcoming "Dark Mode" and "Branns GTFO" features.
- Added a "Create ZipMog Macro" button that dynamically generates a macro to equip/learn all bag items for transmog.
- Added logic to automatically place the generated ZipMog macro onto the player's next available action bar slot.

2026-07-15
- Reviewed libraries and confirmed that Ace3 libraries can be removed as they are not used. Retained LibStub, CallbackHandler-1.0, and LibSharedMedia-3.0.
- Registered all fonts in the `fonts` folder using LibSharedMedia-3.0 so they can be selected anywhere custom fonts are supported.
